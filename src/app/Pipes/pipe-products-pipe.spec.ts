import { PipeProductsPipe } from './pipe-products-pipe';

describe('PipeProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
